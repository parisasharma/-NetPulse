# ── Database ──────────────────────────────────────────────────────────────────
# Local SQLite (default — no setup needed for local dev)
DATABASE_URL=sqlite:///./netpulse.db

# PostgreSQL (use this for Railway / production)
# DATABASE_URL=postgresql://user:password@localhost:5432/netpulse

# ── Email Alerts (Gmail) ──────────────────────────────────────────────────────
# 1. Enable 2FA on your Gmail account
# 2. Create an App Password at myaccount.google.com/apppasswords
# 3. Paste the 16-character app password below (NOT your Gmail password)
ALERT_EMAIL_FROM=your.email@gmail.com
ALERT_EMAIL_PASSWORD=your_16char_app_password
ALERT_EMAIL_TO=alerts@youremail.com

# ── Dashboard ─────────────────────────────────────────────────────────────────
# URL of the FastAPI backend (change for deployed version)
API_BASE_URL=http://localhost:8000
